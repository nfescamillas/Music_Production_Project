# Personal AI Music Production Coach

Local-first Django foundation for an Ableton Live production coaching tool.

## Verify the foundation

After dependencies are installed, run the smoke test with:

```powershell
uv run pytest tests/test_home.py
```
